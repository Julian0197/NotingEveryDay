## 初衷

+ 前后端规范不一致，前端驼峰，后端下划线需要将key递归转化
+ 拥抱ts，代码的静态类型检测，同一个属性可能被用到很多地方，改完一处将造成不可预知的影响
+ vue2时期，对象的key必须提前定义好，否则丢失响应式。提前顶一个数据的model（类），避免重复劳动。
+ 表单提交，涉及到增量提交（删了什么，改了什么，增加了什么），空字符串、null不允许提交等

## 基本概念

+ 自定义class继承ModelBase后，通过new创建的实例，具有ModelBase所有的原型方法。
+ 